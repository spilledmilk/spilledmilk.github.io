// OPACITY FOR BACKGROUND OBJECTS
$('#header').transify({
	opacityOrig:.5,
	opacityNew:.5,
	// fadeSpeed:1000,
	percentWidth:'100%'
});

$('#navigation').transify({
	opacityOrig:.8,
	opacityNew:.8,
	// fadeSpeed:1000,
	percentWidth:'100%'
});


$('#stepone').transify({
	opacityOrig:.2,
	opacityNew:.2,
});

$('#steptwo').transify({
	opacityOrig:.2,
	opacityNew:.2,
});

$('#stepthree').transify({
	opacityOrig:.2,
	opacityNew:.2,
});